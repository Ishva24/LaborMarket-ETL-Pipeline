# dbt Mart Quality Checks

Labor-market marts should validate uniqueness, not-null keys, relationship integrity, accepted work types, salary range sanity, and skill bridge density. These checks catch upstream extraction and parsing errors before dashboard users consume trend metrics.
